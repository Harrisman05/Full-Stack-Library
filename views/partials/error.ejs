<% if (locals.errorMessage != null) { %> 
    <%= errorMessage %>
<% } %>
<!-- = is used to render text, as space is not counted as empty -->
<!-- = is used to render text, as space is not counted as empty -->